@model IEnumerable<Dictamenes.Models.TipoSujetoObligado>

@{
    ViewBag.Title = "Listado de Tipos de Sujeto Obligados";
}

<h2>Listado de todos los Tipos de Sujeto Obligados</h2>
<p class="">
    <a class="btn btn-primary" href="@Url.Action("Create")"><i class="bi bi-plus-lg"></i>      Cargar nuevo Tipo de Sujeto Obligado</a>
</p>
<table  id="dataTable" class="table table-striped table-hover">
    <thead>
        <tr class="table-primary">
            <th>
                @Html.DisplayNameFor(model => model.Descripcion)
            </th>
            <th class="no-sort">
                @Html.DisplayNameFor(model => model.EstaHabilitado)
            </th>
            <th class="no-sort"></th>
        </tr>
    </thead>

    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Descripcion)
                </td>
                <td>
                    <i class="bi @(item.EstaHabilitado ? "bi-check-circle-fill text-success" : "bi-x-circle-fill text-danger" )"></i>
                </td>
                <td class="row justify-content-end mx-0">
                    <a class="btn btn-warning" href="@Url.Action("Edit", new { id = item.Id })"><i class="bi bi-pencil-square"></i></a>
                </td>
            </tr>
        }
    </tbody>


</table>

@section Scripts {

    <script src="https://cdn.datatables.net/1.10.21/js/jquery.dataTables.min.js"></script>
    <script src="https://cdn.datatables.net/1.10.21/js/dataTables.bootstrap4.min.js"></script>

    <script>
        $(document).ready(function () {
            $('#dataTable').DataTable({
                "ordering": true,
                columnDefs: [{
                    orderable: false,
                    targets: "no-sort"
                }],
                "language": {
                    "url": "//cdn.datatables.net/plug-ins/1.10.16/i18n/Spanish.json"
                }
            });
        });
    </script>
}
