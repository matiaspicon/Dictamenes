
@{
    ViewBag.Title = "LogIn";
}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">        
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @*<div class="form-group row">
            @Html.Label("Usuario")
            <div class="col-md-9">
                <input type="text" name="Nombre" />
            </div>
        </div>
        <div class="form-group row">
            @Html.Label("Cuil/Cuit")
            <div class="col-md-9">
                <input type="text" name="cuil" />
            </div>
        </div>
        <div class="form-group row">
            @Html.Label("Contraseña")
            <div class="col-md-9">
                <input type="text" name="pass" />
            </div>
        </div>
        @Html.Hidden("cuil", "123123123")
        @Html.Hidden("pass", "asdasdas")
        @Html.Hidden("idcomp", "1")*@

        <div class="row">
            <div class="col-sm-9 col-md-7 col-lg-5 mx-auto">
                <div class="card border-0 shadow rounded-3 my-5">
                    <div class="card-body p-4 p-sm-5">
                        <h5 class="card-title text-center mb-5 fw-light fs-5">Loguearse</h5>
                        <form>
                            <div class="form-floating mb-3">
                                <label for="floatingInput">Usuario</label>
                                <input name="nombre" type="text" class="form-control" id="floatingInput" placeholder="Ingrese el usuario">
                            </div>
                            <div class="form-floating mb-3">
                                <label for="floatingPassword">Contraseña</label>
                                <input name="cuil" type="password" class="form-control" id="floatingPassword" placeholder="Ingrese la contraseña">
                            </div>
                            <div class="d-grid">
                                <input type="submit" value="Ingresar" class="btn btn-primary m-1" />
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>

    </div>
}
