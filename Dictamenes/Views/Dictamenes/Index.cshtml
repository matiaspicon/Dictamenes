@model IEnumerable<Dictamenes.Models.Dictamen>

@{
    ViewData["Title"] = "Index";
}

<p>
    <a class="btn btn-primary"  asp-action="CargarFile">Create New</a>
</p>
<table class="table table-striped table-hover">
    <thead>
        <tr class="table-primary">
            <th scope="col">Nro GDE</th>
            <th scope="col">Nro Expediente</th>
            <th scope="col">Fecha creacion</th>
            <th scope="col">Asunto</th>
            <th scope="col">Detalle</th>
            <th scope="col"></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
        <tr>
            <td>
                <a class=" link-dark text-decoration-none" asp-action="Details" asp-route-id="@item.Id">@Html.DisplayFor(modelItem => item.NroGDE)</a>                
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NroExpediente)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaCarga)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Asunto.Descripcion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Detalle)
            </td>
            <td>
                <a class="btn btn-outline-warning" asp-action="Edit" asp-route-id="@item.Id">Edit</a> 
            </td>
        </tr>
        }
    </tbody>
</table>
